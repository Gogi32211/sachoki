"""ultra_scan_routes.py — Ultra Scan APIRouter stub.

All live /api/ultra-scan/* endpoints are defined directly in main.py.
This module exists so that main.py can import the router without error;
the router is empty because no routes are registered here.
"""
from fastapi import APIRouter

router = APIRouter()
