# skills/ — versioned snapshots

The LIVE copy of `quant-study` lives in `~/.claude/skills/quant-study/` (personal skill,
visible in every project). This directory is a versioned backup snapshot so the skill
survives machine loss. When the live skill changes, re-copy it here before committing.
